services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /app/node_modules
      - uploads:/app/uploads
    environment:
      NODE_ENV: development
    command: npm run dev
